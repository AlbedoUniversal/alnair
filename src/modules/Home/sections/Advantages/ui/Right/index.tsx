import { Button, SimpleGrid, Text, Title } from '@mantine/core';
import { useTranslation } from 'next-i18next';

export const Right = () => {
	const { t } = useTranslation('advantages');

	return <SimpleGrid>ededdeedede</SimpleGrid>;
};
